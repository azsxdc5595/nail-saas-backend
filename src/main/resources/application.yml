
server:
  port: 8080

spring:
  mail:
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}

  datasource:
    #url: jdbc:oracle:thin:@www.areszone.net:1525/T05B
    url: jdbc:oracle:thin:@localhost:1525/T05B
    username: NSAS
    password: nsas0322
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

mail:
  config:
    path: classpath:mail-config.txt
    
jwt:
  secret: your-super-secret-key-your-super-secret-key
  access-expiration: 900000        # 15分鐘
  refresh-expiration: 604800000    # 7天